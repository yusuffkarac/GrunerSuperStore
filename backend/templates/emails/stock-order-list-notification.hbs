<h2 style="color: #111827; margin-bottom: 20px;">Neue Bestellliste erstellt!</h2>

<p style="color: #374151; margin-bottom: 15px;">
    Eine neue Bestellliste wurde erstellt und wartet auf Ihre Bearbeitung.
</p>

<div class="info-box">
    <h3 style="color: #111827; margin-bottom: 15px;">Listeninformationen</h3>

    <div class="info-row">
        <span class="info-label">Listenname:</span>
        <span class="info-value">{{listName}}</span>
    </div>

    <div class="info-row">
        <span class="info-label">Erstellt am:</span>
        <span class="info-value">{{createdDate}}</span>
    </div>

    <div class="info-row">
        <span class="info-label">Erstellt von:</span>
        <span class="info-value">{{createdBy}}</span>
    </div>

    <div class="info-row">
        <span class="info-label">Status:</span>
        <span class="info-value">{{status}}</span>
    </div>

    {{#if note}}
    <div class="info-row">
        <span class="info-label">Notiz:</span>
        <span class="info-value">{{note}}</span>
    </div>
    {{/if}}
</div>

<div class="info-box" style="max-width: 100%; overflow-x: hidden;">
    <h3 style="color: #111827; margin-bottom: 15px;">Bestellte Artikel ({{itemCount}})</h3>
    <table style="width: 100%; border-collapse: separate; border-spacing: 0 8px;">
        <thead>
            <tr>
                <th style="text-align: left; color: #374151; font-size: 14px; font-weight: 600; padding-bottom: 4px; width: 60%;">Produkt</th>
                <th style="text-align: left; color: #374151; font-size: 14px; font-weight: 600; padding-bottom: 4px; width: 40%;">Menge</th>
            </tr>
        </thead>
        <tbody>
        {{#each items}}
            <tr style="background: #f9fafb; border-radius: 6px;">
                <td style="color: #374151; font-weight: 500; padding: 8px 12px 8px 0;">
                    <div>{{this.productName}}{{#if this.categoryName}} ({{this.categoryName}}){{/if}}</div>
                    {{#if this.barcode}}
                    <div style="color: #6b7280; font-size: 12px; margin-top: 4px;">[{{this.barcode}}]</div>
                    {{/if}}
                </td>
                <td style="color: #6b7280; padding: 8px 0 8px 0; min-width: 90px;">
                    {{this.quantity}} {{this.unit}}
                </td>
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>

{{#if adminOrderUrl}}
<div style="text-align: center;">
    <a href="{{adminOrderUrl}}" class="button">Bestellliste im Admin-Panel anzeigen</a>
</div>
{{/if}}

<p style="color: #6b7280; font-size: 14px; margin-top: 30px;">
    Bitte bearbeiten Sie diese Bestellliste so schnell wie möglich.
</p>

